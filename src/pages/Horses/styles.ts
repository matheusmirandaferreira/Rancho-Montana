import styled from 'styled-components';

export const Container = styled.div`
  padding: 1rem 0;

  .horses {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 3rem;
    justify-content: space-between;
  }
`;
