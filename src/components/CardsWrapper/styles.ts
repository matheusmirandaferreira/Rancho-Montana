import styled from 'styled-components';

export const Container = styled.div`
  .cards {
    margin-top: 1rem;
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 3rem;
    justify-content: space-between;
  }
`;
